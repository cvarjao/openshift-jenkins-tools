pipeline {
    agent none
    parameters {
        booleanParam(defaultValue: true, description: 'Github webhook payload', name: 'payload')
    }
    stages {
        stage("Processing") {
            steps {
                echo "flag: ${params.payload}"
            }
        }
    }
}